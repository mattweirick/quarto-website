```{=html}

<style>
  .btn-xs {
    padding: .07em .31em;
    font-size: .9em;
 
  .ai-lg {
    font-size: 1.2em;  /* Adjust as needed */
  }
  
  ul {
    list-style-type: none;
    margin: 0;
    padding: 0;
  }

</style>

<ul class="list">
  <% for (const item of items) { %>
    <li <%= metadataAttrs(item) %>>
      <%= item.title %> <% if (item.pubclosed) { %> <a href="<%= item.pubclosed %>" title="read the publisher's version of <%= item.title %>"><i class="ai ai-closed-access"></i></a> <% } else{ %> <a href="<%= item.pubopen %>" title="read the open access version of <%= item.title %>"><i class="ai ai-open-access"></i></a> <% } %>
      <br/>
      <% if (item.type === 'Published') { %>
        <small class="text-muted" style="display:block;">
          <%= item.author %>. <%= item.journal %>.
        </small>
      <% } %>
    </li>
  <% } %>
</ul>

<!---
<ul class="list">
  <% for (const item of items) { %>
    <li <%= metadataAttrs(item) %>>
      <%= item.title %>
      <br/>
      <% if (item.type === 'Published') { %>
        <small class="text-muted">
          <%= item.year %>. <%= item.author %>. <%= item.journal %>. <% if (item.pubclosed) { %> <a href="<%= item.pubclosed %>"><%= item.pubclosed %>.</a> <% } else{ %> <a href="<%= item.pubopen %>"><%= item.pubopen %>.</a> <% } %>
        </small>
      <% } %>
    </li>
  <% } %>
</ul>

to add an indent on lines after title: 
style="margin-left:1em;display:block;"
--->



```